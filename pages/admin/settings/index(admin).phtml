<h2>Settings</h2>
<table class="table">
  <?php foreach ($rows as $row): ?>
  <tr>
    <th><?php e($row['settings']['key']);?></th>
    <td><?php e($row['settings']['value']);?></td>
  </tr>
  <?php endforeach; ?>
</table>